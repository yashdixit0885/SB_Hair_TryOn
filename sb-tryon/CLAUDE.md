@AGENTS.md

All coding-agent rules for this codebase live in `AGENTS.md` at the repo root. The file above is loaded automatically by Claude Code via the `@AGENTS.md` directive.

If you are reading this in a tool that does not honor `@`-imports (e.g. Cursor, Copilot Chat), open `AGENTS.md` directly.
